# Generated by rucksack. Commands are names, not secrets.
version: 1
commands:
  python-test:
    command: "python3 -m unittest discover -s tests"
    timeout_seconds: 120
    required: true
  legacy-python-test:
    command: "python3 -m unittest discover -s legacy_tests"
    timeout_seconds: 900
    required: false
  vm-verify:
    command: "infra/vm/verify.sh"
    timeout_seconds: 120
    required: false
