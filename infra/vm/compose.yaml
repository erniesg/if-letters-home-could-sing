# Generated by rucksack. rucksack-ci-compose
# Builds the repository Dockerfile on the trusted VM deploy host.
# Secret values arrive through the environment file passed by deploy.sh.
services:
  app:
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-8000}:8000"
    environment:
      APP_ENV: ${APP_ENV:-production}
    volumes:
      - app-data:/var/lib/rucksack-app

volumes:
  app-data:
